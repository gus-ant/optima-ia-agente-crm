# ============================================================
# Óptima IA — Agente Lara
# Arquivo de variáveis de ambiente — COPIE PARA .env
# NUNCA COMMITE O .env COM VALORES REAIS
# ============================================================

# --- Ambiente ---
ENV=development          # development | production
LOG_LEVEL=INFO
PORT=8000

# --- LLM ---
LLM_MODEL=gpt-4o        # gpt-4o | gpt-4o-mini | claude-sonnet-4-5
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...   # opcional se usar Claude

# --- WhatsApp ---
WHATSAPP_PROVIDER=meta   # meta | evolution

# Meta Cloud API
META_PHONE_NUMBER_ID=
META_ACCESS_TOKEN=
META_APP_SECRET=
META_WEBHOOK_VERIFY_TOKEN=optima_ia_verify_token_aqui

# Evolution API (self-hosted) — ignorado se WHATSAPP_PROVIDER=meta
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=
EVOLUTION_INSTANCE=lara

# --- CRM ---
CRM_PROVIDER=bitrix24    # bitrix24 | hubspot

# Bitrix24
BITRIX24_WEBHOOK_URL=https://seudominio.bitrix24.com/rest/1/XXXXX
BITRIX24_BASE_URL=https://seudominio.bitrix24.com

# HubSpot — ignorado se CRM_PROVIDER=bitrix24
HUBSPOT_API_KEY=

# --- Redis (persistência de sessão) ---
REDIS_URL=redis://localhost:6379

# --- Atendente humano (transbordo) ---
HUMAN_AGENT_WHATSAPP=5511999990000  # número do atendente (sem +)

# --- CORS ---
ALLOWED_ORIGINS=*

# --- Limites de custo LLM ---
# Configure no dashboard da OpenAI/Anthropic — não há controle via env aqui
