# Óptima IA — Dependências Python
# Instalar com: pip install -r requirements.txt

# --- Framework Web ---
fastapi>=0.111.0
python-multipart>=0.0.9
uvicorn[standard]>=0.29.0

# --- LangChain + LangGraph ---
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.8
langchain-anthropic>=0.1.15   # opcional — habilitar se usar Claude
langgraph>=0.1.5
langgraph-checkpoint>=1.0.2

# --- HTTP ---
httpx>=0.27.0

# --- Persistência ---
redis[asyncio]>=5.0.0
psycopg2-binary>=2.9.9        # PostgreSQL — para histórico de longo prazo
sqlalchemy>=2.0.0
asyncpg>=0.29.0               # Driver assíncrono PostgreSQL
aiosqlite>=0.20.0             # Driver assíncrono SQLite para testes locais
langchain-postgres>=0.0.10     # Integração PgVector para LangChain
pgvector>=0.2.0               # Driver PgVector Python


# --- Validação ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Utilitários ---
python-dotenv>=1.0.1
structlog>=24.2.0             # logging estruturado

# --- Integração (v2.0) ---
google-auth>=2.0.0            # Google authentication
google-api-python-client>=2.0.0  # Google APIs (Calendar, etc)
apscheduler>=3.10.0           # Agendador de tarefas (lembretes automáticos)

# --- Dev / Testes ---
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-httpx>=0.30.0
httpx>=0.27.0

# --- RAG e MCP ---
pgvector>=0.2.5
langchain-postgres>=0.0.9
langchain-text-splitters>=0.2.0
pypdf>=4.2.0
python-docx>=1.1.0
mcp>=1.0.0
